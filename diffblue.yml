buildCmd: mvn -e -X -Dlicense.skip=true -Dmaven.enforcer.skip=true  -Denforcer.skip=true -Dair.check.skip-all=true -Dmaven.test.skip=true -pl core -am package
testCmd: true
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
  # Constrain the size of arrays created in the analyzed code.
  java-max-vla-length: 100000
phases:
  -
    # Phase 0 PRETTY, SIMPLE and SMART - pretty tests, do not test private methods directly, Smart-Harness
    timeout: 180
    cbmcArguments:
      # Use simple models first containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Instantiate classes using the simplest constructor strategy
      smart-harness: simplest-constructor-and-nondet
  -
    # Phase 1 PRETTY AND COMPLEX - standard models only, pretty tests, do not test private methods directly, input factory
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
      # Limit input factory recursive depth (default 0 = no recursion allowed)
      java-test-input-factory-bmc-recursion-limit: 4
  -
    # Phase 2 FIFO Strategy - pretty tests, do not test private methods directly, fifo
    timeout: 100
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Add constraint that strings are printable
      max-nondet-string-length: 100
      # Explores the program tree breadth-first.
      paths: fifo
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: false
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 3 Exceptions - do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 4 Direct testing - test private methods directly, increased reflection
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
    # PHASE 5 Mocking - aggressive mocking, do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Only load the class (and related inner/outer classes) containing the method under test,
      load-containing-class-only: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
units:
 - target:
   - dir: common/target/classes
     file: org/keycloak/common/util/PathHelper.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/PathHelper.java
 - target:
   - dir: core/target/classes
     file: org/keycloak/util/TokenUtil.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/util/TokenUtil.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/JWEUtils.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/JWEUtils.java
   - dir: common/target/classes
     file: org/keycloak/common/util/Base64$OutputStream.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/Base64.java
   - dir: core/target/classes
     file: org/keycloak/util/JsonSerialization.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/util/JsonSerialization.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/JWE.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/JWE.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/JWERegistry.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/JWERegistry.java
   - dir: common/target/classes
     file: org/keycloak/common/util/Base64.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/Base64.java
   - dir: core/target/classes
     file: org/keycloak/jose/jws/JWSInput.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jws/JWSInput.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/JWEHeader.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/JWEHeader.java
   - dir: core/target/classes
     file: org/keycloak/representations/RefreshToken.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/representations/RefreshToken.java
   - dir: core/target/classes
     file: org/keycloak/representations/JsonWebToken.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/representations/JsonWebToken.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/JWEKeyStorage.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/JWEKeyStorage.java
   - dir: common/target/classes
     file: org/keycloak/common/util/Base64Url.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/Base64Url.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/alg/JWEAlgorithmProvider.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/alg/JWEAlgorithmProvider.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/JWEException.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/JWEException.java
   - dir: core/target/classes
     file: org/keycloak/jose/jwe/enc/JWEEncryptionProvider.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jwe/enc/JWEEncryptionProvider.java
   - dir: core/target/classes
     file: org/keycloak/jose/jws/JWSInputException.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/jose/jws/JWSInputException.java
 - target:
   - dir: common/target/classes
     file: org/keycloak/common/util/ServerCookie.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/ServerCookie.java
 - target:
   - dir: core/target/classes
     file: org/keycloak/crypto/JavaAlgorithm.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/crypto/JavaAlgorithm.java
 - target:
   - dir: core/target/classes
     file: org/keycloak/Config.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/Config.java
   - dir: core/target/classes
     file: org/keycloak/Config$Scope.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/Config.java
   - dir: core/target/classes
     file: org/keycloak/Config$SystemPropertiesConfigProvider.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/Config.java
   - dir: core/target/classes
     file: org/keycloak/Config$ConfigProvider.class
     sourceDir: core/src/main/java
     sourceFile: org/keycloak/Config.java
 - target:
   - dir: common/target/classes
     file: org/keycloak/common/util/HtmlUtils.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/HtmlUtils.java
 - target:
   - dir: common/target/classes
     file: org/keycloak/common/util/KeycloakUriBuilder.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/KeycloakUriBuilder.java
   - dir: common/target/classes
     file: org/keycloak/common/util/Encode.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/Encode.java
   - dir: common/target/classes
     file: org/keycloak/common/util/PathHelper.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/PathHelper.java
 - target:
   - dir: common/target/classes
     file: org/keycloak/common/util/UriUtils.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/UriUtils.java
   - dir: common/target/classes
     file: org/keycloak/common/util/MultivaluedHashMap.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/MultivaluedHashMap.java
 - target:
   - dir: common/target/classes
     file: org/keycloak/common/util/NetworkUtils.class
     sourceDir: common/src/main/java
     sourceFile: org/keycloak/common/util/NetworkUtils.java
